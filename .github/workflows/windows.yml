name: Windows Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CXX: cl.exe

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - uses: microsoft/setup-msbuild@v1.0.0

    - name: CMake
      run: cmake -G "Visual Studio 16 2019" -A x64 -S . -B build

    - name: Build
      run: cmake --build build

    - name: Run tests
      run: build\Debug\utl-test.exe
